{
  "name": "ts",
  "type": "module",
  "version": "0.1.0",
  "scripts": {
    "esbuild": "esbuild ./src/lib.ts --bundle --platform=neutral  --outfile=./dist/out.js",
    "check-type": "tsc --noEmit",
    "build": "npm run check-type && npm run esbuild"
  },
  "devDependencies": {
    "esbuild": "^0.25.10",
    "typescript": "^5.9.2"
  }
}
